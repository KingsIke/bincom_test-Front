const PollingUnit = () => {
  return (
    <>
      <h1>Welcome</h1>
      <p>
        HERE are the routes # To get polling unit results "/polling-unit-list" #
        To add new polling unit result /add-new-polling" # Get lga's result
        /lga-scores"
      </p>
    </>
  );
};

export default PollingUnit;
